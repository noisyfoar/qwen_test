<UserControl x:Class="ShellExtension.Formats.LIS.Dialogs.Import.View.TabRename"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Выбор переименования кривой"
                   FontSize="15"
                   FontWeight="SemiBold"/>

        <RadioButton Grid.Row="2"
                     Content="Без переименования"
                     IsChecked="{Binding Path=IsNoRename, Mode=TwoWay}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <RadioButton Content="Префикс"
                         IsChecked="{Binding Path=IsPrefix, Mode=TwoWay}"
                         VerticalAlignment="Center"/>
            <TextBox Margin="12,0,0,0"
                     MinWidth="220"
                     Text="{Binding Path=Prefix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal">
            <RadioButton Content="Постфикс"
                         IsChecked="{Binding Path=IsPostfix, Mode=TwoWay}"
                         VerticalAlignment="Center"/>
            <TextBox Margin="12,0,0,0"
                     MinWidth="220"
                     Text="{Binding Path=Postfix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <GroupBox Grid.Row="8" Header="Предпросмотр">
            <TextBlock Margin="8,6,8,6"
                       Text="{Binding Path=PreviewText, Mode=OneWay}"/>
        </GroupBox>
    </Grid>
</UserControl>
