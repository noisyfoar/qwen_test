<!--<UserControl x:Class="NPFGEO.IO.LAS.Import.View.SelectEncoding"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NPFGEO.IO.LAS.Import.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Кодировка">
            <DockPanel>
                <UniformGrid DockPanel.Dock="Top" Columns="4" Margin="0, 2, 0, 2">
                    <UniformGrid.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Padding" Value="4, 0, 4, 0"/>
                        </Style>
                    </UniformGrid.Resources>
                    <Button Content="Win-1251" Margin="0, 0, 2, 0" Command="{Binding Path=SetWin1251Command, Mode=OneTime}"/>
                    <Button Content="Cyr-866" Margin="0, 0, 2, 0" Command="{Binding Path=SetCyr866Command, Mode=OneTime}"/>
                    <Button Content="KOI8-R" Margin="0, 0, 2, 0" Command="{Binding Path=SetKOI8RCommand, Mode=OneTime}"/>
                    <Button Content="UTF-8" Command="{Binding Path=SetUTF8Command, Mode=OneTime}"/>
                </UniformGrid>

                <ListBox SelectedItem="{Binding Path=CurrentEncoder, Mode=TwoWay}"
                      ItemsSource="{Binding Path=Encoders, Mode=OneTime}"
                         SelectionChanged="ListBox_SelectionChanged"
                              HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                              ScrollViewer.VerticalScrollBarVisibility="Visible"
                              ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Opacity="1.0" Text="{Binding Path=DisplayName, Mode=OneWay}"/>
                                <TextBlock Opacity="0.5" Text="{Binding Path=Name, Mode=OneWay, StringFormat={} ({0})}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </GroupBox>

        <GridSplitter Grid.Column="1" Foreground="Transparent" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        <GroupBox Header="Образец" Grid.Column="2">
            <TextBox IsReadOnly="True" Text="{Binding Path=TextExample, Mode=OneWay}" TextWrapping="NoWrap"
                     ScrollViewer.VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto"/>
        </GroupBox>

    </Grid>
</UserControl>-->
