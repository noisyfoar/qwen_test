<UserControl x:Class="NPFGEO.ShellExtension.Formats.LIS.Dialogs.Import.View.TabFormatLIS"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Подтвердите импорт выбранных кривых."
                   FontSize="14"
                   FontWeight="SemiBold"/>

        <GroupBox Grid.Row="2" Header="Выбрано для импорта">
            <ListBox ItemsSource="{Binding Path=SelectedCurves, Mode=OneWay}" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                            <TextBlock Text="{Binding Path=Caption, Mode=OneTime}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Path=Units, Mode=OneTime, StringFormat={} [{0}]}"
                                       Margin="8,0,0,0"
                                       Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</UserControl>
